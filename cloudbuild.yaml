steps:
  # 1. Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Build'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/nexus-app:$COMMIT_SHA', '.']

  # 2. Push the container image to GCR
  - name: 'gcr.io/cloud-builders/docker'
    id: 'Push'
    args: ['push', 'gcr.io/$PROJECT_ID/nexus-app:$COMMIT_SHA']

  # 3. Deploy to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    id: 'Deploy'
    entrypoint: gcloud
    args:
      - 'run'
      - 'deploy'
      - 'nexus-app'
      - '--image'
      - 'gcr.io/$PROJECT_ID/nexus-app:$COMMIT_SHA'
      - '--region'
      - 'us-central1'
      - '--allow-unauthenticated'
      - '--port'
      - '8080'
      - '--set-secrets'
      - 'GEMINI_API_KEY=GEMINI_API_KEY:latest'
      - '--set-env-vars'
      - 'BQ_DATASET=nexus_audit,GCS_BUCKET=$PROJECT_ID-attachments'

images:
  - 'gcr.io/$PROJECT_ID/nexus-app:$COMMIT_SHA'

options:
  logging: CLOUD_LOGGING_ONLY
